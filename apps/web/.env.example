# Postgres connection string.
# Local dev: `docker compose up -d db` then use the URL below.
# Production: paste the pooled connection string from Neon (Vercel storage tab).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/portfolio

# Canonical site URL (inlined into the client bundle at build time).
NEXT_PUBLIC_SITE_URL=https://sandesh.site
